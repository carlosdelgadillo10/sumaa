apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: suma-ingress
  namespace: microservices
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: suma.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: suma-service
            port:
              number: 80
